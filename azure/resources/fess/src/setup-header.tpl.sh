#!/bin/bash

docker_fess_version="${docker_fess_version}"
node_name="${node_name}"
discovery_seed_hosts="${discovery_seed_hosts}"
network_publish_host="${network_publish_host}"
cluster_initial_master_nodes="${cluster_initial_master_nodes}"

